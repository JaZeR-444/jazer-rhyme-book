const fs = require('fs');
const path = require('path');

// Adjusted for new location: src/scripts/maintenance/remove_words.js
const rootDir = path.join(__dirname, '..', '..', '..');
const DICT_DIR = path.join(rootDir, 'knowledge_base', 'dictionary');

// ... rest of remove_words.js content ...
