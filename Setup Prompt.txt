SYSTEM / ROLE
You are operating as a senior information architect + data-modeling engineer. Your job is to produce a clean, scalable local folder + JSON infrastructure for a creative “knowledge hub” optimized for rap/music writing retrieval and long-term expansion.

PRIMARY OBJECTIVE
Generate the complete initial local infrastructure for a knowledge base named:
"JaZeR Master Flow Knowledge Hub"

The output must be:
(A) a production-ready folder tree
(B) JSON schema templates
(C) starter index files
(D) README + conventions
(E) optional helper scripts for validation + index regeneration

SCOPE LIMIT (IMPORTANT)
Do NOT mention Notion, Notion databases, or Notion mapping. This prompt is strictly for local folder infrastructure + JSON standards.

HARD REQUIREMENTS
1) Use a “Domains → Entities → Indexes → Relations” architecture.
2) Create ONE JSON file per entity (people, tech, cinema, lingo, music, etc.).
3) Provide a universal base schema shared by all entities + domain-specific extensions.
4) Include cross-linking via related_ids and explicit relations maps.
5) Optimize for: quick writing retrieval and long-term scalability.
6) Include slug/id rules that enforce uniqueness and readability.
7) Include alias handling (alias_map indexes).
8) Include tag governance (controlled vocabulary) with tags.json and tag rules.
9) Every file must be valid JSON (no trailing commas, no comments).

DELIVERABLE FORMAT
Return everything as:
1) A folder tree (text)
2) A list of files to create with FULL contents for each file in separate code blocks
3) Keep file contents concise but complete and correct

FOLDER TREE (MUST CREATE)
Create this root folder exactly (spaces allowed):
JaZeR Master Flow Knowledge Hub/
  README.md
  schemas/
  data/
  exports/
  scripts/
  _meta/

DATA DOMAINS (MUST INCLUDE)
data/
  people/
  tech/
  cinema/
  lingo/
  music/
  brands/
  places/
  internet-culture/
  writing-tools/

WITHIN EACH DOMAIN (MUST INCLUDE)
entities/
indexes/
relations/

SCHEMAS (MUST INCLUDE)
schemas/
  base.entity.schema.json
  people.entity.schema.json
  tech.entity.schema.json
  cinema.entity.schema.json
  lingo.entity.schema.json
  music.entity.schema.json
  brand.entity.schema.json
  place.entity.schema.json
  meme.entity.schema.json (for internet-culture)
  writing-tool.entity.schema.json

BASE ENTITY FIELDS (MUST INCLUDE)
id, type, name, aliases[], era, tags[], one_liner, angles[], bar_seeds[], related_ids[], created_at, updated_at, sources[]

DOMAIN-SPECIFIC FIELDS (MUST INCLUDE)
people: known_for[], vibe, signature_items[], quotables_short[], controversy_flags[]
tech: category, what_it_is, why_it_matters, risks[], metaphors[], keywords[]
cinema: plot_hooks[], iconic_scenes[], visual_motifs[], themes[], quotables_short[]
lingo: meaning, usage, origin, region, example_lines[], do_not_use_if[]
music (bands/artists): genre[], era_detail, signature_songs[], sonic_descriptors[], influence_tree[], aesthetic
brand: category, price_tier, signature_products[], status_signals[], bar_angles[]
place: type (city/venue/district), vibe, sensory_cues[], cultural_refs[], bar_angles[]
meme: format, origin_platform, meaning, remix_rules[], lifespan_risk, example_lines[]
writing-tool: category, what_it_does, best_for[], workflow_hooks[], pitfalls[], bar_angles[]

STARTER FILES (MUST INCLUDE)
- data/*/indexes/alias_map.json (empty object starter)
- data/*/indexes/by_tag.json (empty object starter)
- data/*/indexes/by_era.json (empty object starter)
- data/*/relations/relations.json (empty array starter)
- _meta/tags.json (include initial controlled vocab tags across domains: 40+ starter tags)
- _meta/id_rules.json (slug rules, casing, separators, uniqueness rules)
- _meta/relation_types.json (e.g., inspired_by, references, similar_to, opposite_of, associated_with, originated_in, popularized_by, used_in, depicted_in, produced_by, sampled_by)

README.md (MUST INCLUDE)
- What this repo is for
- Folder conventions and naming
- How to add new entities
- How to link entities (related_ids + relations.json)
- How to keep tags clean (controlled vocab)

SAMPLE DATA (MUST INCLUDE)
Create 2 sample entity files per domain (9 domains = 18 samples) with realistic example content.
- Keep samples culturally neutral and avoid long copyrighted quotes.
- Use plausible, short “quotables_short” that are paraphrased or generic (no long direct quotes).

SCRIPTS (OPTIONAL BUT RECOMMENDED)
scripts/
  validate.js (validate JSON files + schemas; fail on errors)
  build-indexes.js (regenerate by_tag/by_era/alias_map from entities)

TECHNICAL CONSTRAINTS
- Prefer Node.js for scripts.
- Schemas should use JSON Schema Draft 2020-12 or Draft 07 (choose one and be consistent).
- Enforce required fields and type enums where appropriate.
- Use ISO-8601 for created_at and updated_at.

STYLE / TONE
Be concise, implementation-oriented, and strict about correctness. Avoid fluff.

NOW EXECUTE
Generate the folder tree and all required file contents.
