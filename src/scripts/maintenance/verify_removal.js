const fs = require('fs');
const path = require('path');

// Adjusted for new location: src/scripts/maintenance/verify_removal.js
const rootDir = path.join(__dirname, '..', '..', '..');
const DICT_DIR = path.join(rootDir, 'knowledge_base', 'dictionary');

// ... rest of verify_removal.js content ...
